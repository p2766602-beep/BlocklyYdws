# Blockly 互動教學平台 — 規劃摘要

最後更新：2026-07-23（情境B確認為資優班課程定位，主題由11個擴充為13個並定案學習順序）

> **本文件是 BlocklyYdws、YDWS-CodingBank、YDWS-EscapeRoom 三專案共用的跨專案歷史記錄**，
> 實體檔案放在 BlocklyYdws 裡。不管變更發生在哪一側，跨專案會影響到的決策都要回來這裡補記錄，
> 並更新下面這張現況表——這樣任何一次對話（不管在哪台電腦、開哪個專案）都能先看這張表確認
> 現況，不需要重讀整份時間序歷史才能拼出目前狀態。詳見三專案各自的 `CLAUDE.md`。

## 目前決策現況（2026-07-20 更新，取代逐輪翻找）

| 主題 | 現況 | 真正待辦 |
|---|---|---|
| 難度標籤系統 | Master schema=**L1~L5**（2026-07-18定案）。YDWS-CodingBank內部工具（CSV+`web/problem-set-builder.html`）**完整支援L1~L5篩選**，341筆全數有難度等級+評分理由（0缺漏，2026-07-20補完最後13題）。 | BlocklyYdws**學生端**顯示：僅L1/L2/L3已有對照標籤（`"L1｜基礎"`等，11個已上線課程套用），L4/L5尚無標籤（對應課程JSOPT01/JSSIM01等還沒匯出成JS）；原四級用詞「初階」去向未定。範圍小，不影響題庫篩選/覆核工作。 |
| 重複匯入題目canonical化 | 第八輪10組（356→344）+ 2026-07-20查獲並合併1組W0-04系列3題（344→341）+ 2026-07-21全面重掃剩餘43個W系列、合併6組逐字重複（341→335，已push）。`W0-01`/`W0-03`經查證**其實是第八輪就已合併過的舊案例**（不是新發現）。**2026-07-21再對全部335筆做跨批次全面掃描，只剩3個低信心度候選，逐一讀原文確認皆非重複（不同運動項目的相似題、demo/practice變體）——題庫目前確認乾淨，無更多重複。** | 無（已完結）。**設計原則已釐清**：題庫CSV不可重複，但**同一題內容匯出到JS後在不同課程/同課程重複出現不算問題**（不需要處理`JSB02.js`/`JSS01.js`裡的重複ID）。 |
| 題庫索引檔新鮮度 | **已解決**：2026-07-21已重新執行`build_problem_bank_index.py`+`simplify_problem_bank_index.py`+`build_problem_content_index.py`，索引檔（335筆）確認不再含已刪除的6個重複ID，已commit push。 | 無。之後只要再改CSV，記得重跑這三個腳本。 |
| BlocklyYdws GitHub Pages部署 | **已是最新**：gh-pages分支2026-07-20 11:29部署，晚於main最新commit（10:37），已包含課程按需載入修正。 | 無，之後有新commit記得重新`npm run deploy`。 |
| 課程資料按需載入 | 已修正（第九輪），改用Vite code-splitting，不再eager整包送進瀏覽器。 | 無。 |
| 雙GitHub帳號設定（p2766602-beep） | 家用電腦已設定`credential.useHttpPath`。 | 學校電腦是否已完成同樣設定，需使用者確認（見`docs/HOME_TO_SCHOOL_SYNC_HANDOFF.md`）。 |
| AI互動模組規劃 | **2026-07-21～22 情境C的MVP已完成、上線測試通過、正式部署到GitHub Pages**（見第十四～十七輪）。後端：`workers/ai-companion/`已部署到Cloudflare Worker（`https://blocklyydws-ai-companion.tnjboxing.workers.dev`），代理Gemini API（`gemini-flash-lite-latest`），system prompt讀`docs/AI伴學GEM-V62-AI素養評估版.md`產生、與金鑰皆不在前端曝光；courseCode白名單+三層用量上限已實測擋人成功。前端：`src/aiCompanion.js`，仿SmartRing模擬器做成可拖曳、**可縮放**浮動視窗（面板`resize:both`、輸入框`resize:vertical`，兩者都已修正並實測正常），並加上AI/學生頭像（`public/avatars/`，已從1.5MB原始檔壓縮到約52KB／160x160px）。自動帶入當前task題目脈絡，換子任務自動重置對話；學生的程式碼/測資結果維持不自動帶入。已用JSA00課程端對端測試多輪，AI回應完全符合V6.2的格式與引導邏輯。**已push+`npm run deploy`到正式GitHub Pages網址**（`https://p2766602-beep.github.io/BlocklyYdws/`），commit依序為`a79b580`／`24c876c`／`b030ef7`（頭像）／`b44bc2a`（縮放修正），全部已在遠端main。**2026-07-22第十八輪：情境B（密室逃脫）架構規格已定案**，決定獨立成新專案（詳見`docs/密室逃脫獨立專案-架構規格.md`）；情境D（SRC00）維持留在主平台AI伴學面板，透過iframe嵌入獨立專案的密室引擎（比照SmartRing模擬器的postMessage模式），不搬進獨立專案也不塞進情境C的簡單聊天Worker。 | **2026-07-23：課程白名單改成跟前端隱藏課程概念對齊的兩層規則**，已更新`wrangler.toml`+`workers/ai-companion/src/index.js`並重新部署Worker：新增`PUBLIC_COURSE_CODES`（鏡射`src/courses/index.js`的`publicCourseGroups`完整清單，兩邊要手動同步）跟`ALLOWED_PUBLIC_COURSE_CODES`（公開課程裡實際開放AI伴學的子集，目前僅`JSA00`）；不在`PUBLIC_COURSE_CODES`裡的課程代碼一律視為隱藏課程，自動放行AI伴學，不用逐一列白名單（跟隱藏課程本身一樣，非安全機制，只降低曝光，額度上限仍由三層rate limit把關）。情境B獨立專案的repo已建立（`p2766602-beep/YDWS-EscapeRoom`，private，家用電腦clone在`C:\YOSEP\YDWS-EscapeRoom`，2026-07-22，含跨專案CLAUDE.md骨架），Cloudflare Worker/KV命名、embed.html實際串接測試、實際遊戲引擎程式碼皆待動工。教師後台/Google Sheet記錄對話與回顧仍未做（MVP階段刻意先不做）。**2026-07-23第二十輪：情境B確定採遊戲化「軟性推薦路徑」總覽（不鎖關），MVP只做技能樹骨架，美術地圖化延後；第二十一輪：確認為資優班課程定位，主題由11個擴充為13個並定案學習順序（詳見`docs/密室逃脫獨立專案-架構規格.md`§1/§8/§9）**。 |
| cyjunior-002原始txt缺陷 | 已修正（第四輪），BlocklyYdws與YDWS-CodingBank兩處raw txt皆同步。 | 無。 |
| Claude Code開發輔助設定（2026-07-23新增） | 三專案（BlocklyYdws／YDWS-CodingBank／YDWS-EscapeRoom）都加了`.claude/skills/handoff/SKILL.md`：換機器/離開前的收尾流程，會依序檢查三專案git狀態、列出未commit/未push變更，**等使用者明確同意才push或部署**，不會自動動作。BlocklyYdws另外加了`.claude/settings.json`的PostToolUse hook：編輯`src/courses/`底下的`.js`/`.json`時自動跑`npm run build`（純build驗證，未過濾出`gh-pages`部署），build失敗才會跳出訊息，已用真實課程檔案實測過fire/不fire兩種情況。順便補commit了資料夾搬家（`C:\cProject\BlocklyYdws`→`C:\YOSEP\BlocklyYdws`）後CodingBank/EscapeRoom兩邊`CLAUDE.md`跟`export_course_js.py`當時已改好但沒commit的路徑同步。 | 題庫難度重評流程（`COURSE_MAP_DESIGN.md`的L1-L5關鍵字比對＋`需要人工複核`旗標）評估後認為值得做成類似的checklist skill（暫定`/audit-difficulty`），但因為判斷本質是質性閱讀不是可算的信心分數，還沒動工，等下次要做大批次難度重評時再建。 |

## 目標客群
國小 5 年級至國中 9 年級，約 30 人規模（單班/單梯次估算）。

## 核心概念
Blockly 拖拉積木教學平台；內建預設課程；導入 AI 擔任伴學/助教；支援 SmartRingController 硬體與純軟體模擬器雙軌。

**2026-07-18 決定（reverse先前7/18當天稍早的移除決定）：硬體功能保留**，不再移除 SmartRingController（ESP8266）連線與韌體，理由是既有 `blockly-lab` 專案已經把硬體整合得很完整、且有純軟體模擬器可解決 30 人課堂規模的裝置成本問題，拿掉反而浪費既有成果。

**2026-07-18 決定：不從零開發，改以既有專案為起點升級優化**：

| 專案 | 路徑 | 角色 |
|---|---|---|
| `blockly-lab` | `C:\YOSEP\blockly-lab` | 既有、已部署（GitHub Pages）的教學平台本體：Vite+Google Blockly、SmartRing硬體/模擬器、系統評分引擎、Google Sheet成績上傳、課程JS動態載入。**決定以此為新平台程式碼起點**，移植進新repo。 |
| `YDWS-CodingBank` | `C:\YOSEP\YDWS-CodingBank` | 既有、獨立的**題庫生產管線**（原始txt→清理→人工資料表→課程JS→匯出到blockly-lab），README明確定位「題庫生產工具」與「課程發布平台」分離，且註明不建議公開整包題庫（含測資與答案）。**決定沿用此管線繼續生產/維護題庫**，不重寫轉換工具。 |
| `BlocklyYdws` | `C:\cProject\BlocklyYdws` | 本次規劃討論的暫存/文件區，尚待決定最終角色（見「尚待討論」）。 |

## 已定案的技術架構

- **積木引擎**：Google Blockly（`blockly` npm套件，非Scratch），blockly-lab已用Vite+Blockly 13.0實際建置部署過。
- **硬體/模擬器雙軌**：
  - 實體：ESP8266 SmartRingController + WebSerial（115200 baud，JSON逐行協定），韌體、協定文件、Blockly積木（連線/LED控制/暫存陣列/動畫DEMO）全部已在blockly-lab中實作並曾部署測試。
  - 模擬：`blockly-lab/public/smartring-simulator/`（`embed.html`純CSS渲染12顆LED圓環、`bridge-test.html`測試面板）+ `src/smartring/simulator-bridge.js`，可讓學生不需實體裝置也能操作，是解決30人規模成本問題的關鍵。
- **AI 服務**：採API串接（非iframe嵌入ChatGPT/Gemini網頁版），傾向**Gemini API**。金鑰存後端環境變數；後端需驗證學生身分；不開放學生自帶API Key。（此決策不受硬體決定reverse影響，維持原判斷）
- **主機**：前端GitHub Pages/Cloudflare Pages；後端金鑰代理用Cloudflare Workers/Vercel Functions。blockly-lab目前用`gh-pages`套件部署到GitHub Pages，可沿用同一套部署方式。

## 費用評估（粗估，需以實際試跑量修正）
以 30 人、週 2 小時課、輕量問答型互動估算，使用便宜/快速模型層級 + prompt caching，
一學期費用量級落在數百至一兩千元台幣，屬可負擔範圍。
**注意**：若採用「密室逃脫檢核」這類長篇敘事型多輪對話，單次互動 token 用量明顯高於陽春問答，
建議定案後實測幾次真實對話再回頭修正預算，不要只靠估算。

## 模組列表

### 核心教學模組
1. Blockly 積木程式設計模組（沿用blockly-lab既有Blockly編輯器）
2. 課程內容模組（課程JS動態載入，沿用blockly-lab `import.meta.glob`自動掃描機制，比手動註冊index.js更好維護）
3. 程式解題練習模組（題庫 + 自動批改 + AI引導；批改引擎已在blockly-lab實作：攔截console.log/alert/prompt輸出、逐筆比對testCases）
4. SmartRing 硬體連線與模擬模組（**2026-07-18恢復**；實體WebSerial + 純軟體模擬器雙軌，沿用blockly-lab既有實作）

### AI 互動模組（共用後端，多套情境設定）
5. AI 互動引導模組
   - 情境設定 A：自由問答伴學
   - 情境設定 B：密室逃脫概念檢核引擎（見 `prompts/密室逃脫檢核-GEM.md`，通用5關Socratic驗證引擎，可掛載不同主題知識包）
   - 情境設定 C：Blockly 解題過程陪伴——**採用「AI伴學-Blockly解題陪伴.md」4階段版（I/O拆解→概念連結→條件規則釐清→人工大腦兵推）為權威版本（2026-07-18使用者確認）**。另一份`BlocklyLab_GPT_System_Instructions_AI_Literacy_V2.md`（Q1~Q5流程）不採用為主要版本，先保留參考，如未來需要國中/國小分版可能回頭取材。
   - 情境設定 D（新增）：SRC助教——12個SmartRing示範動畫復刻密室，掛載情境設定B的引擎、換用`SRC_GPT_Minimal_Knowledge_V3.md`知識包。**決定整合進平台AI模組，不維持獨立ChatGPT自訂GPT**（理由：獨立GPT要求13歲以上帳號，與目標客群衝突；且目前SRC00課程任務要求學生在平台與GPT間來回切換，整合後可消除此摩擦；也能比照密室逃脫引擎把關卡狀態交給後端掌管）。

### 系統管理模組
6. 帳號與角色管理（學生/教師）——blockly-lab目前無此功能，需新建。
7. 學習歷程追蹤（進度、解題紀錄、AI互動紀錄、教師後台總覽）——需新建，可能與成績Google Sheet資料整合。

### 基礎建設模組
8. API 代理與安全後端（金鑰保護、用量控管）——需新建（AI伴學專用，Google Sheet走Apps Script另計）。
9. 主機部署（前端靜態 + 後端 serverless）——沿用blockly-lab既有GitHub Pages部署方式。

### 評分與成績匯出模組（**已在blockly-lab完整實作，非從零開發**）
- 系統評分：讀取task的`testCases`，逐筆帶入、攔截輸出、比對expectedOutput，結果表格化顯示。
- Google Sheet上傳：`google-apps-script/Code.gs`（Apps Script後端）+ 前端no-cors POST，欄位含時間/班級/座號/姓名/課程/題目/模式/分數/通過筆數/通過率/是否全部通過。
- **待補強**：`Code.gs`的`doPost`目前沒有任何來源驗證，任何取得Web App URL的人都能塞假資料進Sheet；且前端testCases含expectedOutput直接打包進公開GitHub Pages原始碼，學生可透過檢視原始碼看到答案——競賽模式(`mode:contest`)正式上線前建議評估後端評分或至少加上傳驗證token。

### 題庫課程組管理模組
- 課程代碼 → 課程組 → 學生輸入代碼解鎖。
- **2026-07-18更新：題庫生產改沿用YDWS-CodingBank既有六階段管線**（見下方專節），不再自行設計txt→JS轉換流程。

## AI 互動設計的工程風險備忘
1. 遊戲關卡進度/正確答案判定，建議由後端狀態機掌管，不要純靠 prompt 指示 AI 自律，避免長對話漂移或被學生套話跳關。（SRC助教整合進平台後同樣適用）
2. 隨機亂數（如密室逃脫的三位數質數金鑰）建議後端程式產生，不要交給 AI。
3. Blockly 解題陪伴的「截圖判讀」建議搭配平台自動帶入積木 XML/JSON 結構，比純圖像辨識準確。
4. AI協作回顧觸發，建議串接後端批改結果（真正通過測試案例才觸發），不要只信任學生自報「完成了」。
5. Prompt 洩漏防禦是盡力而為，非絕對保證，教育情境風險可接受。

## 既有題庫整合（2026-07-18 重大更新：改用YDWS-CodingBank既有管線）

**先前判斷已過時**：原本以為要自行盤點`YDWS-CodingData`（181個txt）並設計轉換流程，後來發現使用者已有一個完整獨立專案`YDWS-CodingBank`專門做這件事，且已經運作到相當成熟的階段。

### YDWS-CodingBank 管線現況

六階段流程：`raw/`原始txt → `scan_raw_problem_files.py`掃描 → `import_txt_problems.py`初步解析成`cleaned/problems_json/` → 人工整理`data/problem_bank_master.csv`（中文欄位主表）→ 依`data/course_map.csv`與`data/problem_groups.csv`規劃課程與組題 → `build_course_js.py`產出`output/courses/` → `export_course_pack.py`匯出到blockly-lab。

**已完成成果（PB06批次）**：12個課程組、共300題，已通過三輪自動稽核（補題完成度、命名一致性、Blockly匯入前格式）皆為**錯誤0、警示0**。建議交付檔案：`BSA01.js / BSC01.js / CNT01.js / STA01.js / EXT01.js / IDX01.js / STR01.js / SORT01.js / SIM01.js / MATH01.js / GREEDY01.js / GRAPH01.js`——這正好對應BlocklyYdws/courses資料夾裡先前發現「index.js未註冊的13個檔案」中的12個，**證實BlocklyYdws/courses資料夾的內容其實是YDWS-CodingBank管線的部分輸出**，不是獨立來源，也解答了先前「difficultyLevel/review/risk等欄位從何而來」的疑問。

**權威schema文件**：`YDWS-CodingBank/docs/PROBLEM_BANK_SCHEMA.md`，中文欄位主表涵蓋題目基本資料、課程規劃、難度與教學定位、能力標籤、輸入輸出結構、Blockly適配、品質檢查七大類，比之前列的JS欄位列表更完整（那份是最終輸出格式，這份是上游主資料表規格）。

**題庫缺口分析已存在**：`docs/PROBLEM_BANK_GAP_ANALYSIS.md`已列出後續補題建議方向（二分搜尋、前綴和、滑動視窗、頻率表、集合去重、堆疊佇列、圖論入門、動態規劃暖身、除錯型、輸出格式控制等），與先前建立的`知識點與學習次序.md` Tier2/3內容高度呼應，值得交叉比對。

**txt污染問題已有因應機制**：`PROBLEM_BANK_GAP_ANALYSIS.md`品質風險清單明確列出「題目宣告測資筆數與實際案例數是否一致」為匯入時必查項目，管線的匯入/稽核工具已考慮此問題，不需要另外手動清理181個txt。

**原始檔案系列統計**（來自README）：115J系列5個、A系列26個、TYTN系列12個、WP系列8個、W系列46個、count系列18個、cy系列36個、nanto系列18個、select系列12個，共181個——這只是數量盤點，各系列在課程地圖裡的教學定位仍需查`data/course_map.csv`才能確認。

### 新發現的衝突：難度分級標準不一致

YDWS-CodingBank的`PROBLEM_BANK_SCHEMA.md`規格是**難度等級L1~L5**（對應學習階段：入門/基礎/進階/競賽基礎/挑戰），已有300題實際用這套標記完成三輪稽核。這跟2026-07-18稍早決定的「基礎/初階/進階/挑戰」四級**不是同一套**。這是本輪新出現、尚未請示使用者的分歧，需要決定：延用L1~L5（好處：300題已經標記好，不用重做），還是改四級（好處：符合先前決定，但要重新對應300題的分級規則）。

## 尚待討論/確認事項

**已解決（原5題）**：
1. ~~`export_course_js.py`存放位置~~ → 找到於`YDWS-CodingBank/tools/`，但正式產出管線實際用的是`build_course_js.py` + `export_course_pack.py`（`export_course_js.py`可能是較早期或平行的工具，需要時再細看差異）。
2. ~~`index.js`未註冊的13個檔案~~ → 已確認其中12個是YDWS-CodingBank PB06批次的正式交付物，應直接依SOP複製過去，不是「排除中」或「草稿」。
4. ~~txt污染清理~~ → YDWS-CodingBank管線的匯入/稽核工具已處理，300題已通過品質稽核，不需手動清理。

**部分解決**：
3. difficultyLabel分級 → 不只是「補初階」的問題，而是要先決定L1~L5 vs 基礎/初階/進階/挑戰四級（見上方新發現的衝突）。
5. W系列/115J/TYTN/nanto系列定位 → 已有數量統計，但教學定位（週次序列 vs 比賽考古題）需要查`YDWS-CodingBank/data/course_map.csv`才能確認，尚未實際查閱。

**2026-07-18第二輪決定**：
11. ~~難度分級標準~~ → **決定沿用L1~L5**。但使用者指出「之前系統難度設定部分題目有問題」，
    **需要重新檢核修正**，不是單純沿用就結束。已發現YDWS-CodingBank自己就有一份
    `output/reports/classification_review_report.md`（由`build_classification_review_report.py`產生），
    列出總題數202、Blockly適配低33題、L4/L5高難度33題、需人工複核2題的候選清單（含具體題目ID與
    建議處理方向，例如多筆競賽題被標L4但建議檢查是否可調整為中）。**待確認：這份既有報告是否就是
    使用者所指的問題清單，還是使用者手上另有更明確的錯誤清單？**
**2026-07-18第三輪更新**：
- 新GitHub repo已建立：`https://github.com/p2766602-beep/BlocklyYdws.git`（新帳號，非tnjbox），
  已確認為空repo（`git ls-remote`無任何ref，且無需認證即可讀取，代表repo為public）。
- 題目顯示規則定案：題目名稱／題目說明／範例格式（表格）三者**必須顯示**；輸入格式說明／輸出格式
  說明**只有原始題目確實有標示才顯示，沒有就省略**，不得留空標題或用範例格式頂替。
- **格式解析bug已找到具體案例並確認根因**：以`blockly-lab/src/courses/JSB02.js`裡的「115-1-考試獎勵」
  為例，實際檢查發現——`description`欄位結尾被截斷在一個孤立的「【」字元、`inputDescription`開頭卡著
  對應的「】」、`outputDescription`結尾把整段「範例格式／使用者輸入／輸出／範例說明」原始表格文字都
  黏了進去（導致跟`examples`陣列內容重複顯示）。**根本原因**：原始txt題敘內文本身使用「【兌換規則】」
  這種裝飾性中括號做重點強調，但轉換工具的段落切分邏輯誤把題敘內文中的中括號當成區塊分隔符號，
  導致description/inputDescription/outputDescription被從錯誤的位置切開，範例格式區塊也沒有正確跟
  輸出說明區隔開。**確認只有A、count、select三個原始txt系列轉換乾淨，其餘系列（W/WP/115J/TYTN/
  nanto/cy等）都有類似風險，需要系統性重新檢查**，非AI直接手寫的題目（如BSA01/BSC01/CNT01等PB06
  批次）不受影響。**待辦**：需要到`YDWS-CodingBank/tools/`裡找出實際負責段落切分的腳本
  （候選：`parse_problem_raw_text.py`、`import_txt_problems.py`）修正這個bug，而不是逐題手動修正。

12. ~~Repo整併策略~~ → **決定**：`blockly-lab`維持原地不變動（不搬移、不改動）；`YDWS-CodingBank`
    留在原地但需要修正難度對應錯誤與部分格式解析錯誤（待確認具體是哪份報告/哪些題目，候選：
    `output_format_fix_review.md`、`course_output_format_audit.md`、`raw_text_parse_report.md`）；
    `BlocklyYdws`要在**新的GitHub帳號**下建立新repo（注意：是新帳號，不是tnjbox帳號下的新repo）。
    **待確認**：新帳號是否已建立？若尚未建立，帳號申請需要使用者本人手動完成（Claude無法代為註冊
    GitHub帳號），建立後把帳號/repo資訊提供給我即可繼續設定remote。
    **機密內容保護具體作法（建議）**：新repo只放平台程式碼（從blockly-lab移植）+ 已定案匯出的課程JS
    （必然含testCases/expectedOutput，見下方13的說明，使用者已接受此風險），**不放YDWS-CodingBank的
    raw/cleaned/data等上游工作資料**（該專案本來就主張不公開，維持獨立於新repo之外）；`.env`/API金鑰
    照原訂規則排除；另需檢查blockly-lab原始git歷史中有無測試用真實學生資料需要一併排除。
13. ~~競賽模式內容安全性~~ → **使用者確認可不處理**：目前僅止於學生練習，非正式競賽，testCases含
    答案打包進公開原始碼的風險可接受。使用者順帶釐清的功能規格——**競賽模式評分只顯示正確/錯誤，
    學習模式才顯示學生輸出 vs 正確輸出的完整比對**——經查`blockly-lab/src/main.js`（1679~1734行）
    **已經是現有實作**：`isContestMode`成立時會顯示提示文字「競賽模式僅顯示每筆測資是否通過，不顯示
    輸入、預期輸出與實際輸出」，並隱藏對應欄位。移植到新repo時原樣保留即可，不需要額外開發。
14. ~~`Code.gs`的`doPost`要不要加上傳驗證token~~ → **已加**：`UPLOAD_TOKEN`（Script Property，選填）+
    `SCORE_UPLOAD_TOKEN`（`src/main.js`，選填），任一邊沒設就不檢查，不影響現有測試。

**2026-07-19進度**：
- JSL01/JSA01/JSS01/JSB02格式bug已修復（重跑`YDWS-CodingBank/tools/export_course_js.py`現行版本，
  該工具自己的修正紀錄第7項就是這個bug，只是這4個課程是用修正前的舊版跑的，重跑即可，不用改程式碼）。
  已複製回`BlocklyYdws/src/courses/`並push。連帶產生一個`JSA02.js`（0題空課程），先不採用。
- `SCORE_UPLOAD_URL`已填入（沿用使用者現有的Apps Script Web App端點）。
- `vite.config.js`的`base`路徑修正為`/BlocklyYdws/`（原本沿用blockly-lab的`/blockly-lab/`會導致資源404）。
- 已`npm install` + `npm run build` + `npx gh-pages -d dist`部署，網站應可在
  `https://p2766602-beep.github.io/BlocklyYdws/` 看到（若GitHub Pages設定尚未完成需要使用者手動去
  repo Settings → Pages 確認來源分支是`gh-pages`）。
- 待辦：`classification_review_report.md`難度候選清單是否採用，尚待使用者確認。

**2026-07-19第二輪：難度重新評設任務啟動**

- 使用者說明歷史成因：初始AI分類時為了讓L1~L5題數平均分布，沒有真正依題目難度判斷；A/count/select三系列
  是真正的入門題，其餘大部分是競賽題被錯誤分級。後續使用者自行補了一批知識點入門題直接寫成JS（即PB06批次）。
- **確認範圍**：全部重新評設，不考慮題數平衡，完全依實際難度對應。範圍涵蓋：
  1. 現有JS上線的txt衍生題（JSA01/JSB02/JSL01/JSS01，115題）
  2. PB06知識點入門題（BSA01等14組，108題）
  3. blockly-lab原生手寫題（CPB00/JSA00/JSB00，44題，使用者確認一併納入）
  4. 已分類但未上線的CSV題目（JSB01/JSG01/JSGR01/JSM01/JSOPT01/JSSIM01，87題，使用者確認一併納入、
     透過課程代碼開放，不需公開列表——**平台的隱藏題組+課程代碼解鎖機制是刻意設計**，用來保護題目與
     製造過關儀式感，非漏洞）
  總計約362題待重評。
- **評分標準（已提案，待最終確認）**：對照`知識點與學習次序.md`的Tier1→2→3，依題目實際需要的知識點/
  技能判斷難度，而非題敘長短：L1=Tier1第1-4項、L2=Tier1第5-9項、L3=Tier2巢狀迴圈/函式/清單/線性搜尋/
  字串/二維資料/氣泡選擇排序、L4=Tier2進階或Tier3入門（插入排序/二分搜尋/貪婪法）、L5=Tier3進階
  （DFS/BFS/動態規劃/圖論）。
- **重大發現：既有CSV管線有資料遺失風險**——追查`data/problem_bank_master_tagged.csv`產生鏈發現它
  同時是`auto_tag_problem_bank.py`的**衍生輸出**，又被`apply_classification_fixes.py`/
  `import_problem_additions.py`直接**手動patch**，兩種角色衝突。實證：`output/reports/
  BSA01_PB06_import_report.md`（2026-07-06）記載PB06的108題當時確實寫入了這份CSV，但**現在檢查
  該CSV完全沒有BSA01**，代表後續某次重跑auto_tag_problem_bank.py把patch進去的資料洗掉了。
- **決定**：新建一份完整的題庫主CSV（涵蓋上述全部約362題，含目前完全沒有CSV代表的PB06批次與
  blockly-lab原生批次），取代舊的`problem_bank_master_tagged.csv`作為主資料表，避免同樣的資料遺失
  再發生；既有的稽核/報表/格式檢查工具視情況保留沿用，只換掉主資料表這一層。
- 待辦：使用者確認評分標準表、以及是否採用`--dangerously-skip-permissions`或改用權限允許清單，
  確認後開始執行362題的批次重新評估（規模大，預計分批用子任務處理，先出總表給使用者覆核再套用）。

**2026-07-19第三輪：362題難度重評已完成、總表已產出待覆核**

- 已在`C:\cProject\.claude\settings.local.json`設定權限允許清單（git/python/node/npm常用操作+三個
  專案資料夾讀寫），未採用`--dangerously-skip-permissions`，危險操作（force push/大量刪除等）仍會詢問。
- 已建置完整題庫主表：`YDWS-CodingBank/data/problem_bank_master_complete.csv`（362題，整合txt競賽題
  202+PB06知識點入門題108+blockly-lab原生題44+三來源標記），做為後續統一資料源。
- 用6個並行子任務依課程分組（依`知識點與學習次序.md`Tier對照評分），完成全部362題難度重新評估，
  產出總表Artifact（sortable/filterable HTML）：包含現有難度→建議難度、判斷依據、信心度(高/中/低)。
  統計：建議變更165題、高信心度228題、中信心度108題、低信心度26題待人工覆核。
- **重評過程中意外發現3類資料完整性問題，需使用者決定處理方式**：
  1. 疑似重複匯入題目7組（`cycjunior-004≡W5-04`等），高度暗示W系列可能是其他競賽系列的重複打包，
     呼應先前未解的「W系列定位」疑問。
  2. ID衝突1組：`cyelement-005`同時代表兩題不同內容，需重新命名。
  3. `cyjunior-002`發現跟JSB02/JSS01同款的括號截斷格式bug，難度暫無法可靠判斷，需先修復。
- 待辦：使用者覆核總表後決定是否套用，套用方式需再議（改寫`problem_bank_master_complete.csv`的
  難度欄位、同步回JS課程檔、處理上述3類完整性問題）。

**2026-07-19第四輪**

- **重複匯入不是bug，是刻意設計澄清**：使用者說明——題庫CSV的題目本身不可重複（同一題只該有一筆
  canonical資料），組出的JS課程代碼檔名不可重複，但**同一題內容可以被組進不同課程代碼的題目組**。
  之前發現的7組「重複匯入」，正確處理方向應是：master CSV裡合併成同一筆canonical資料，改記錄
  「這題被哪些課程代碼使用」，而不是當成錯誤刪除。165題難度建議套用時，使用者要求先保留，下次再處理，
  所以這個合併動作也還沒執行。
- **cyjunior-002複查**：使用者已手動修改`C:\cProject\BlocklyYdws\YDWS-CodingData\cyjunior-002-
  神秘的古代石板.txt`，但複查後**發現raw txt本身仍有內容缺陷，不是先前的括號截斷parser bug**：
  第10-14行有兩個重複的`<h3>輸入格式</h3>`標題，內容互相矛盾——第一個正確描述輸入（超長數字字串），
  第二個實際在描述輸出（兩個0/1標記位是否為5的倍數/11的倍數），懷疑是原作者誤把「輸出格式」標題
  複製貼上成「輸入格式」。這是內容層級問題,需要使用者確認正確意圖後修正,不是我能自行判斷代改的。
- **GitHub Pages模擬器404已修復**：`src/smartring/simulator-bridge.js`的`getBasePath()`寫死
  判斷`/blockly-lab/`路徑，換新repo（`/BlocklyYdws/`）後恆常失配，導致模擬器iframe網址404。
  已改用Vite的`import.meta.env.BASE_URL`（對應`vite.config.js`的`base`設定，不寫死repo名稱），
  重新build+部署，已push（commit 9b1b28a）。
- 165題難度建議套用：**使用者要求本次先保留，下次確認後再處理**。
- **cyjunior-002已修正**：確認是複製貼上遺漏，第二個「輸入格式」標題已改為「輸出格式」，
  BlocklyYdws與YDWS-CodingBank兩處raw txt都已同步修正。這題難度之後補進重評總表即可，
  不用整批重跑。

**2026-07-19第五輪：165題覆核啟動，高信心度68題已套用**

- 直接讀取Artifact存檔的完整362筆原始JSON重新分析（不透過摘要模型），發現165筆「建議變更」其實混了
  兩種性質：**44筆是首次評設**（blockly-lab原生題CPB00/JSA00/JSB00，本來就沒有難度值，非改判爭議）；
  **121筆才是真正重評**（現有難度→建議難度確實不同），其中高信心度68（調升32/調降36）、中信心度48
  （43筆有讀原文但把握不到高信心、5筆純靠標題/同名題推定）、低信心度5筆明確待人工複核。
- **使用者決定：先套用高信心度68題**，其餘（44首次評設、48中信心度、5低信心度）留待下次處理。
- 已執行：`YDWS-CodingBank/data/problem_bank_master_complete.csv`68筆難度欄位全部更新（逐筆核對CSV現值
  與總表記錄的現有難度一致，0筆不符，已備份`.bak-20260719`）；其中52筆課程代碼已上線的JS課程檔
  （JSA01/JSL01/JSB02/JSS01/BSA01/CNT01/GRAPH01/GREEDY01/IDX01/CYELEMENT01/CYJUNIOR01共11檔）同步
  更新對應欄位（txt衍生課程用`difficulty`欄位，PB06知識點課程用`difficultyLevel`+`difficultyLabel`
  兩欄位一起改，L1/L2/L3對應「基礎/進階/挑戰」）；`node --check`語法驗證全過、`git diff --stat`
  60增60刪精準對應68次異動無誤觸。其餘16筆（JSM01 2、JSOPT01 1、JSSIM01 13）課程尚未匯出成JS，
  只更新了CSV，等之後正式匯出時會帶著新難度值一起出去。
  **已本機commit（587e553），使用者要求先不push不部署**，等後續確認再繼續。
- **意外解開一個懸案**：先前稽核報告列的「`cyelement-005` ID衝突，同時代表兩題不同內容」經直接查CSV
  後**證實是誤報**——是重評子任務把`cyelement-005`（停車費計算）跟`cycelement-005`（多一個c，無人搬運車
  運送計算）看錯搞混，`cyelement-005`本身穩定對應同一題「停車費計算」，只是同時被JSB02和CYELEMENT01
  兩個課程代碼引用，符合「同一題可組進不同課程」的既有設計，不是真正的ID衝突。7組疑似重複匯入題目
  的合併與canonical化仍待處理（不受此發現影響）。

**2026-07-19第六輪：44題首次評設（CPB00/JSA00/JSB00）已覆核套用**

- 44題原本是`YDWS-CodingBank/data/problem_bank_master_complete.csv`裡難度等級空白的blockly-lab原生題
  （CPB00 18題、JSA00 12題、JSB00 14題），不是改判既有值，風險比68題那批低。已用Artifact整理成表格
  （分組+信心度篩選）供覆核，29題高信心度、15題中信心度，L1~L4分佈13/19/8/4，**使用者確認全部44題
  直接套用**。
- 已執行：`problem_bank_master_complete.csv`44筆「難度等級」「難度說明」欄位全部由空白填入建議值
  （逐筆比對課程代碼+題目ID，44/44精準命中，0誤觸）；`BlocklyYdws/src/courses/CPB00.js`、`JSA00.js`、
  `JSB00.js`三個課程檔同步新增`difficulty`欄位（這三個課程原本完全沒有難度欄位，是首次加入非改值），
  `node --check`語法驗證全過，`git diff --stat`18+12+14=44次插入精準對應44題無誤觸。**尚未commit**，
  等使用者確認後再進下一步（commit/push/部署）。
- 165題覆核進度更新：44首次評設已完成，剩121題真正重評中的48題中信心度、5題低信心度待處理；
  7組重複匯入題目合併也仍待處理。

**2026-07-19第七輪：44題首次評設commit + 121題重評全數完成（讀原文覆核，非盲套建議值）**

- 44題首次評設（第六輪）與先前68題文件說明一起commit（3abc8f5）。
- 逐一讀取原始txt/課程JS原文，確認/修正剩餘的12題「未讀原文」低信心度項目（含cyjunior-002轉檔污染疑慮
  證實為誤報，污染只在無關的樣板案例6-10，不影響正式測資）。
- **系統性掃描發現16組「同一題目ID跨課程代碼難度不一致」**，比先前稽核報告的「7組」規模更大：A系列
  （JSA00原生 vs JSM01/JSS01衍生）7題、cyelement系列（JSB02 vs CYELEMENT01）7題、cyjunior系列
  （JSSIM01 vs CYJUNIOR01）2題。逐題讀原文核對後全部對齊（含撤回先前對A-09-1的獨立誤判、cyelement-005
  跟W7-03三方比對後下修對齊、cyjunior-003讀完貪婪演算法原文後改上修對齊）。
- 副產物發現`JSB01`是`JSB00`前6題的過期未匯出重複拷貝（無對應.js檔），**已刪除**（`problem_bank_master_complete.csv`
  與`course_map.csv`各移除相關列，刪除前已備份`.bak-20260719-2309xx`）。
- 剩餘48題中信心度、5題低信心度全數處理完畢：31題套用建議值、2題（TYTN-03、JSSIM01-P02）讀原文後判斷
  維持現值不採納建議、其餘經一致性比對確認無需變動。套用過程又抓到2組新的跨課程不一致（`cyjunior-006`、
  `cyjunior-009`各自對到JSG01/CYJUNIOR01的第二個副本），已一併讀原文核實對齊。
- 全部改動已同步`BlocklyYdws/src/courses/`下9個課程檔（`node --check`全過，`git diff --stat`50增50刪
  精準對應）與`YDWS-CodingBank/data/problem_bank_master_complete.csv`（362→356筆，扣除JSB01的6筆）。
  已commit（59b0b14）。
- 165題覆核（連同121題重評的擴充範圍）至此全部處理完畢。

**2026-07-19第八輪：重複匯入題目合併（使用者授權離線期間自主執行，範圍限定CSV層級，不動已匯出JS）**

- 使用者授權在其離線期間直接執行後續工作，明確界線：僅本機操作不push/部署（延續全程做法），且範圍
  限定`YDWS-CodingBank/data/problem_bank_master_complete.csv`的canonical化整理，不去動已經匯出上線
  的課程JS檔（那屬於「從已上線課程刪除一題」的更大決定，原規劃從未要求這麼做）。
- 重新掃描「同課程代碼+同題名+不同題目ID」，找到**10組**（比先前初估的「7組」更精確，也比稽核報告
  另外提到的「16組」跨課程難度不一致範疇小——這10組是同一課程內的純重複匯入，16組是同一題被拆進不同
  課程代碼但難度各自評估不一致，兩者是不同性質的問題，已分開處理）。逐組讀取原始txt直接diff內容
  （非僅比對標題），全部10組確認為真重複（同規則同範例，部分連測資都逐字相同）。
- Canonical選擇原則：內容逐字相同時預設保留cy*/nanto*系列ID（假設為原始批次，W系列為後續重新打包）；
  若在cy*/nanto*版本發現明顯轉檔污染（如「智慧溫室的自動監控」nantoJS-001範例說明誤留一句與本題無關
  的「總計180元」；「量子訊號的接力傳輸」nantoJS-004結尾誤留另一題的「找出這組最優配對」字句），改選
  品質較佳的W系列版本為canonical；「神秘的煉金術配對」三份重複中選題敘最完整、明確標註數值範圍限制的
  W9-04。
- 執行：canonical列的「備註」欄位加註合併紀錄（含被合併ID與理由），刪除其餘12筆重複列，
  `problem_bank_master_complete.csv`356→344筆（356是扣除JSB01後的基準）。已檢查`problem_groups.csv`
  無懸空引用被合併ID。刪除前已備份`.bak-20260719-2326xx`。
- 至此362題完整難度重評+7(10)組重複匯入合併全部處理完畢，題庫稽核與整理工作告一段落。

**2026-07-20第九輪：課程資料改為按需載入，修正「隱藏課程」實際上整包外洩的問題**

- 使用者釐清了三層分類：**公開課程**（`JSB00`/`JSA00`/`CPB00`等，UI選單有列出）、**不公開資料**
  （`YDWS-CodingBank`的題庫總表CSV，只留本機，不上GitHub）、**隱藏課程**（`MATH01`/`SIM01`/`SORT01`/
  `JSB02`/`CYELEMENT01`等，JS已上傳部署但UI選單不主動列出，輸入正確課程代碼才載入——這是刻意設計，
  非資料外洩）。
- 但查證發現：`src/courses/index.js`原本用`import.meta.glob(eager:true)`把所有課程檔（含隱藏課程的
  完整題目、測資、答案）在頁面載入當下就整包打包進主要JS bundle，UI選單的「公開/隱藏」只是顯示層級的
  區分，**跟資料有沒有送到瀏覽器完全無關**——不需要真的輸入正確代碼，打開瀏覽器開發者工具就能看到隱藏
  課程的完整內容。這個曝光在這次修正之前就已經存在（`BlocklyYdws`與`YDWS-CodingBank`兩個repo都是公開
  repo，GitHub Pages也已上線），不是這次改動造成的新問題。
- 使用者確認隱藏課程目前是教學/練習用途（非計分競賽），接受「無法做到絕對防護（GitHub Pages是純靜態
  託管，送到瀏覽器的東西必然能被看到），但可以避免『一般使用者不用做任何事就整包拿到』」這個折衷方案，
  採用**方案一**：把課程資料改成按需（輸入正確代碼才）動態載入，而非放棄GitHub Pages改走後端方案。
- 已執行：`import.meta.glob`拿掉`eager:true`，每個課程檔案改由Vite各自切成獨立chunk；`getCourseGroup`/
  `getAllCourseGroups`改為async動態載入；`getPublicCourseGroupListHtml`改用純靜態的
  `fallbackPublicCourseTitles`，不再需要載入任何課程模組就能顯示選單。修正過程中一併抓到並修掉一個因
  拿掉舊有`courseGroups`物件殘留參照造成的console error。
- **已用Playwright headless瀏覽器實際跑過完整驗證**（不只是build成功）：`npm run build`後檢查`dist/`
  輸出，25個課程檔全部各自獨立成chunk，主bundle（914KB）不再含任何課程testCases/description字串；
  實際跑`vite preview`+瀏覽器操作確認：(1)初始頁面載入只抓主bundle+CSS，不抓任何課程chunk；(2)輸入
  公開課程代碼`JSB00`只抓`JSB00`那一個chunk，內容正確渲染；(3)輸入隱藏課程代碼`MATH01`只抓`MATH01`
  那一個chunk，8個子任務正確列出；(4)輸入錯誤代碼顯示正確的「找不到課程組」訊息；(5)全程console零
  錯誤。已commit（08af00a），**尚未push/部署**（後續已於同日部署，見上方現況表）。

**2026-07-20第十輪：學校端YDWS-CodingBank工作補記 + W系列重複匯入查證合併 + 建立跨專案提醒機制**

- **補記一筆先前漏掉的紀錄**：使用者今天稍早在學校對YDWS-CodingBank做了以下工作，但當時的對話
  沒有回來更新這份共用日誌，導致回家後的對話一度誤判「難度標籤系統還沒做完」——這正是本輪要修正
  的結構性問題本身的一次實例：
  1. 網頁組卷工具（`web/problem-set-builder.html`）三項強化：課程代碼顯示、題目內容預覽Modal、
     必選題勾選（commit `6673f30`）。
  2. 修正組卷匯出PB06知識點課程題目（如`IDX01-008`）內容空白的bug（`make_task()`備援邏輯，
     commit `64da82f`）。
  3. 補齊13題（26筆）缺漏的難度評分理由，`cyjunior-008`（書架修繕工程）依`COURSE_MAP_DESIGN.md`
     的二分答案關鍵字定義，上修L4→L5（commit `522803f`）。**至此341筆全數有難度等級+評分理由，
     0缺漏。**
  4. 另修正`nanto-006`/`nantoJS-006`系列ID格式錯誤、`CPB00`(count系列)18題ID格式錯誤
     （commit `8c9a560`/`65b7c27`）。
- **W系列重複匯入查證**：`8c9a560`的commit訊息標記了一個待查疑慮——「`nantoJS-006-4`與
  `W0-04-4`標題相同但ID不同，可能是W系列重複打包」。逐字比對「星際物資運補任務」4小題系列後確認：
  位置1（神祕的配重）、2（疏散飛船的乘客名單）、4（防禦塔的能量負載）3題核心規則與範例數字完全
  一致，是同題改寫重複匯入；位置3（`W0-04-3`物流中心選址 vs `nantoJS-006-3`火星樣本回收）解法、
  輸入輸出格式、課程代碼皆不同，確認是誤報（只是剛好都被作者放在系列第3個位置）。
- 依第八輪canonical原則（cy*/nanto*為原始批次、W系列為重新打包，兩邊皆無轉檔污染），保留
  `nantoJS-006-1/2/4`為canonical，刪除`W0-04`/`W0-04-2`/`W0-04-4`重複列，
  `problem_bank_master_complete.csv`344→341筆，canonical列備註加註合併紀錄。已確認
  `course_map.csv`/`problem_groups.csv`無懸空引用。全部8筆現況皆為`題目狀態=draft`，對應課程
  （JSGR01/JSOPT01）皆未匯出成BlocklyYdws的JS，零風險。已commit（`90dcb00`）並push。
- **尚未系統性查證**：`W0-01`（智慧溫室的自動監控）、`W0-03`（量子訊號的接力傳輸）是否也有對應的
  重複原始批次（第八輪的稽核報告提過這兩題nantoJS-001/nantoJS-004版本有轉檔污染跡象，但沒確認
  是否真的存在對應的W系列重複）。
- **建立跨專案提醒機制**：使用者指出核心問題——兩專案是獨立git歷史，只讀其中一個會看不到另一個的
  更新，導致同一個問題（如難度標籤系統）在學校/家裡的不同對話裡反覆被重新提出討論。解決方案：
  1. `BlocklyYdws/CLAUDE.md`與`YDWS-CodingBank/CLAUDE.md`各建一份，互相point到對方，說明如何找到
     姊妹專案路徑（依電腦而異）、以及「回答狀態類問題前必須先查姊妹專案」的固定指示。CLAUDE.md
     會被git追蹤、跟著push/pull走，不需要在兩台電腦上分別手動設定。
  2. 本文件（`專案規劃摘要.md`）最上方新增「目前決策現況」表，取代「要翻完整份時間序歷史才能拼出
     現況」的做法，兩專案共用同一份（實體檔案在BlocklyYdws這邊，YDWS-CodingBank的CLAUDE.md有
     point過來）。
  3. 提醒：Claude的私有跨對話記憶系統是依專案目錄分開存放的，換目錄開對話就讀不到，**不能靠它
     解決這個問題**，必須靠CLAUDE.md這種會跟著git走的機制。

**2026-07-21第十一輪：W系列重複匯入全面重掃，發現2組已上線正式課程重複**

- 延續第十輪的方法，使用者要求對CSV裡剩餘的43個W開頭題目做**全面重掃**（不只是手動核對個案）。
  先查證`W0-01`/`W0-03`：確認**這兩個其實是第八輪就已合併過的舊案例**（備註欄已有2026-07-19的
  合併紀錄），不是新發現，先前列入待辦清單是誤判，已更正。
- 全面重掃方法：把題名去除系列位置後綴（如「(6-4)」「-4」「4-」開頭編號）後做相似度比對，
  只比對「W系列 vs 非W系列」的跨批次組合（同批次內的系列子題如cy*-1~4本來就該不同，不能拿來比），
  同課程代碼內找候選，逐組讀原始txt確認內容。
- 找到**6組**確認為逐字100%相同的重複匯入（比第十輪的「同題改寫」更嚴重，是完全沒改過的複製）：
  `W1-04`≡`nanto-006-4`（碼易丁飲料店-4）JSB02、`W1-05`≡`cycelement-006-4`（寶可夢訓練師-4）
  JSSIM01、`W7-01`≡`cyjunior-006`（校園密室逃脫-書架的密碼6-1）JSS01、`W7-04`≡`cycjunior-006`
  （園遊會攤位熱度分析）JSSIM01、`W7-05`≡`cycjunior-006-3`（園遊會人潮高峰期）JSSIM01、
  `W7-06`≡`cycjunior-006-4`（園遊會場地復原大作戰）JSOPT01。
- 依canonical原則保留後者、刪除W系列重複列，`problem_bank_master_complete.csv`341→335筆，
  已檢查`course_map.csv`/`problem_groups.csv`無懸空引用。已commit（`999d442`）並push。
- **重要發現，跟先前案例不同**：`JSB02`與`JSS01`是**已上線的正式課程**（不像先前案例都還在草稿
  階段），直接查`BlocklyYdws/src/courses/JSB02.js`與`JSS01.js`確認**兩個ID目前都同時存在**——
  代表學生現在打開這兩個正式課程，「碼易丁飲料店-4」跟「書架的密碼」這兩題各會重複出現一次。
  使用者決定：**本輪先只合併CSV層級，不動已上線的JS**（沿用第八輪「不從已上線課程刪題」的既定
  範圍界線），是否要從已上線課程移除重複題目，待使用者另行決定，**列入下次待辦**。
- `cyjunior-006`這個ID同時存在於`JSS01`（草稿，本次合併對象）與`CYJUNIOR01`（已上線，同一題被
  兩個課程代碼共用的既有設計）兩列，已確認只加註`JSS01`那一列，`CYJUNIOR01`列未受影響。

**2026-07-21第十二輪：修正export_course_js.py的BlocklyYdws備援路徑寫死bug（家用電腦匯出會悄悄漏測資）**

- 使用者在YDWS-CodingBank用網頁組卷工具匯出`A20260721.js`（8題自訂題目卷），檢查發現4題
  （`SORT01-006`/`IDX01-004`/`SIM01-007`/`EXT01-006`，皆為PB06知識點課程原生題，無raw txt來源）
  `testCases`是空陣列，但`export_problem_set_course_js.py`自己的匯出報告卻顯示「找不到parsed/raw
  JSON：0」，沒有察覺異常。
- **根因**：`YDWS-CodingBank/tools/export_course_js.py`的`BLOCKLYDWS_COURSES_DIR`寫死成
  `D:\YOSEP\BlocklyYdws\src\courses`（學校電腦路徑）。這次是在**家用電腦**（無D槽，
  `BlocklyYdws`實際在`C:\cProject\BlocklyYdws`）執行，路徑不存在導致這個「parsed/raw JSON查不到
  時，退而找已上線BlocklyYdws課程JS取內容」的備援機制整個悄悄失效，4題因為沒有raw txt來源、
  又找不到備援，最終testCases掛空。因為判斷「找不到內容」的條件是`description`與`testCases`
  同時為空才算異常，這4題description另有來源不是空的，所以報告沒有把它們列成異常。
- **已修正**：改成候選路徑清單（`C:\cProject\BlocklyYdws\src\courses`與
  `D:\YOSEP\BlocklyYdws\src\courses`都列入，`YDWS-CodingBank/CLAUDE.md`已有的兩台電腦路徑對照），
  執行時挑第一個實際存在的路徑，不管在哪台電腦跑都能正確找到備援內容。順帶修掉同檔案docstring
  裡的一個無害but會在PowerShell `2>&1`下被誤判成執行失敗的`SyntaxWarning`（`\e`無效跳脫序列，
  改成雙反斜線）。
- 重新執行匯出後`A20260721.js`已全部8題`testCases`正確帶入（來源即`BlocklyYdws/src/courses/`裡
  這4個課程已上線的正式內容），`validate_course_js.py`的檢查邏輯手動套用結果為錯誤0、警示4
  （4題缺`expectedOutput`別名欄位，只有`output`——查證`BlocklyYdws/src/main.js:1643`評分引擎本來
  就有`testCase.expectedOutput ?? testCase.output ?? ''`容錯，不影響實際評分，純粹是新版校驗器的
  相容性提醒）。
- **提醒**：`YDWS-CodingBank/tools/validate_course_js.py`的`COURSE_CODES`是寫死清單
  （`JSL01`/`JSA01`/`JSS01`/`JSB02`），不會自動涵蓋像`A20260721`這種網頁組卷工具產生的自訂課程，
  之後若常態性用這個工具產生自訂題目卷，可考慮把驗證器改成掃描`output/courses/*.js`全部檔案，
  而非侷限於白名單，避免類似bug再次被漏檢。此次僅修了路徑bug，未改動驗證器涵蓋範圍。

**2026-07-21第十三輪：A20260721測試課程本機驗證後清除**

- 延續第十二輪修好匯出路徑bug後重新產生的`A20260721.js`（8題），在BlocklyYdws本機（`npm run
  dev`，`http://localhost:5173/BlocklyYdws/`）用課程代碼載入實測：子任務下拉選單8題全數正確列出
  （`SORT01-006`/`IDX01-004`/`cyelement-003`/`A-13-0`/`A-09-1`/`115J-03`/`SIM01-007`/
  `EXT01-006`），直接讀取匯出的course JS逐題檢查`description`/`inputDescription`/
  `outputDescription`，確認皆無裝飾性「【】」誤切或截斷問題（`115J-03`題敘本身含「【闖關遊戲進行
  方式】」屬合法強調用法，未被誤判為區塊分隔）；「系統評分」按鈕在空白工作區下也能正常跑完
  不當機，正確回報未通過。
- 測試目的（驗證路徑bug修正後的匯出內容可正常上架）已達成，**依規劃直接清除測試產物，不保留
  在正式輸出**：YDWS-CodingBank端刪除`output/courses/A20260721.js`與對應匯出報告
  （commit `baf0afc`，已push私有repo）；BlocklyYdws端刪除`src/courses/A20260721.js`
  （commit `baa9c4c`，**已push公開repo** p2766602-beep/BlocklyYdws）。另外YDWS-CodingBank根目錄
  有一份無關的殘留檔`test0721.json`（更早一次題目卷匯出測試、10題版本，時間戳與本次8題版不同），
  一併清除，未進git無需commit。
- 兩專案工作區目前皆為乾淨狀態（YDWS-CodingBank僅剩`data/*.bak-*`備份檔案未追蹤，屬工具執行
  副產品，非待辦）。

**2026-07-21第十四輪：AI互動模組（情境C）MVP範圍定案，題庫端工作收尾後轉向**

- 使用者確認YDWS-CodingBank題庫生產管線本身工作已完結（難度標籤、重複匯入合併、索引新鮮度、
  cyjunior-002修正皆已收尾），轉向規劃AI互動模組。
- 使用者提供既有GEM設計成品`docs/AI伴學GEM-V62-AI素養評估版.md`（V6.2，精簡對談＋AI素養鷹架
  增強版）與一段Gemini分享對話（記錄V3→V4→V5→V6.2的完整設計演進），要求先讀懂設計脈絡再提
  規劃草案。
- **關鍵發現：版權風險已在GEM設計階段解決**。V3版原本想在GEM後台內建全國各縣市競賽題目資料庫，
  學生輸入題名即可比對，但分析發現這類競賽題目（不同於依法舉行之考試試題）仍受著作權保護，
  公開分享的GEM若內建完整題庫涉及未授權重製。**最終解法：GEM完全不存題庫，改為要求學生自行
  複製貼上題目**，重製責任在使用者，GEM本身只提供原創教學邏輯（V4版後定案並沿用至V6.2）。
  這與YDWS-CodingBank「題庫不公開、不上這個repo」的既定政策完全對齊，代表AI互動模組後端
  **不需要另建題庫檢索/RAG**。
- **GEM設計演進脈絡**：V3（知識庫比對版，有版權疑慮）→V4（改學生自貼題+模組化五大架構：
  角色定義/任務流程/限制紅線/術語規範/情境應對）→V5（新增「學習概念連結」階段，四階段解題程序；
  加入AI素養評量機制，依《臺灣中小學教師與學生AI素養框架》設計三面向規準）→V6.2（精簡對談
  總原則，回顧輸出改用「徽章轉譯」降低評量感如【🌟卓越主導者】、動態剪裁只挑1-2個亮點+1個
  待改進，不硬列全部向度）。
- **對應規劃表既有的情境分流**：V6.2正是「情境C：Blockly解題陪伴」的核心system prompt，且是
  四種情境中工程風險最低者——純對話式Socratic引導，不像情境B（密室逃脫）/D（SRC助教，套用
  B的引擎換知識包）需要後端狀態機管關卡鎖定、防跳關、隨機質數金鑰生成。**決定MVP先做情境C**，
  B/D留到有狀態機基礎建設後再做。
- **三個整合設計問題已拍板**：
  1. 題目脈絡傳遞：學生做平台正式課程題目時（非自貼競賽題），**後端自動把當前task的
     description/examples帶入context**，不要求學生手動複製貼上（體驗優先，且是自家題庫非他人
     競賽題，版權風險低）。
  2. 對話與AI素養回顧記錄：**MVP階段先不留存教師後台/Sheet**，只在學生端前端顯示，待驗證AI
     伴學本身的教學價值與學生接受度後再決定是否要串Google Sheet（與尚待確認的Sheet同步時機/
     結構問題掛鉤，見下方「其他待確認事項」）。
  3. 後端API代理部署：**Cloudflare Workers**（與基礎建設模組原規劃一致）。
- **下一步待實作**（尚未動工）：Cloudflare Workers後端骨架（代理Gemini API呼叫、藏system prompt
  與金鑰、避免被瀏覽器開發者工具直接看到）+ BlocklyYdws前端AI伴學對話面板。

**2026-07-21第十五輪：Cloudflare帳號完成註冊、存取控制模型定案**

- 使用者確認手上已有Gemini API金鑰，但無Cloudflare帳號。以Google帳號（tnjboxing@gmail.com）
  完成Cloudflare註冊，`npx wrangler login`完成OAuth授權，`wrangler whoami`確認
  Account ID `a97630feec3dc47879b850b6cfbd95a5`。已在BlocklyYdws建立
  `workers/ai-companion/`目錄準備放Worker專案（尚未寫程式碼）。
- **學生帳密/自帶API Key的疑問已解決**：沿用專案規劃摘要最早「已定案的技術架構」段落的既有決定
  ——不建學生帳密系統（30人規模班級/梯次場景不成比例，沿用現有班級/座號/姓名文字欄位識別即可）、
  不開放學生自帶API Key（國中小學生大多無法申請金鑰，且會讓用量完全失控），金鑰與system prompt
  只存在Cloudflare Worker端。
- **釐清「AI能不能自動讀到學生作答情形」**：題目說明/範例可以自動帶入（見第十四輪），但**學生
  目前的程式碼與系統評分結果不自動帶入**。理由：V6.2 GEM的「情境B：已開始解題但卡關」明文要求
  學生自己描述「目前完成哪些積木、卡在哪一步、預期與實際結果」，這對應AI素養評量規準的「面向B：
  問題描述與提問品質」——這是刻意設計的訓練目標，不是疏漏。若後端自動把程式碼/測資結果塞給AI，
  學生完全不用開口描述，這個核心訓練目標會被架空，回顧階段給的徽章也會失真。
- **存取控制模型定案**：GitHub Pages本身完全公開（無登入牆），現在AI功能會產生Gemini API實際費用，
  必須加防護。決定採用三層：①課程代碼白名單（Worker端伺服器驗證，只有指定開放AI功能的courseCode
  能觸發呼叫，不足以防護的地方是代碼外流後仍可被誤用）②用量上限（每課程代碼/session的輪數或
  token上限，防止單一來源燒爆額度）③總額度硬上限（Gemini/Google Cloud帳單用量警報或硬停，最後
  一道安全網）。使用者選擇不加「班級通關碼」這類需要學生額外輸入、可重置的機制（保護性更高但
  多一道輸入步驟、教師也要多管理定期換碼），先以較輕量的三層組合上線。
- 題目資料來源決定：Worker不另外維護一份題目資料庫，改由**前端呼叫時把當前task的description/
  examples放進請求body一併送給Worker**，避免題庫資料兩邊維護不同步（前端本來就有這份資料，不必
  複製一份到後端）。
- 下一步：實作Worker（`workers/ai-companion/`，代理Gemini API、courseCode白名單檢查、rate limit）
  + BlocklyYdws前端AI伴學對話面板，尚未動工。

**2026-07-21第十六輪：AI互動模組情境C的MVP實作、部署、端對端測試全部完成**

- **後端**：`workers/ai-companion/src/index.js`實作完成——CORS來源白名單、courseCode白名單（伺服器端
  驗證）、三層用量上限（KV計數器：學生每小時30輪／課程每天500輪／全站每天2000輪）、組裝Gemini
  請求（system prompt + 題目脈絡以偽造的user/model turn塞在對話最前面 + 對話歷史 + 新一輪輸入）。
  System prompt由`scripts/gen-system-prompt.mjs`自動從`docs/AI伴學GEM-V62-AI素養評估版.md`產生
  `src/systemPrompt.js`（此md檔本身這次才第一次進版控，先前一直是本機未追蹤檔案）。
- **部署過程踩到的坑**：①Windows Git Bash的curl跟PowerShell 5.1的Invoke-RestMethod都對
  `*.workers.dev`回報SSL/TLS handshake失敗，後來查出是workers.dev子網域剛註冊、憑證還在全球
  傳播中，等一下就正常，不是程式問題。②`GEMINI_MODEL`原訂`gemini-flash-latest`實測回503
  「high demand」，改用`gemini-flash-lite-latest`穩定可用且更省成本，已改為預設值。③首次
  `wrangler deploy`卡在「需要註冊workers.dev子網域」的互動式確認，非互動環境預設答「no」會
  失敗；改用`printf 'y\n' | npx wrangler deploy`餵入確認即可過關，自動配發子網域
  `tnjboxing.workers.dev`。
- **前端**：`src/aiCompanion.js`——原本做成置中modal彈窗，使用者測試後要求**改成跟SmartRing模擬器
  一樣的可拖曳浮動視窗**（不擋背景操作、可與模擬器同時開啟），已仿照
  `src/smartring/simulator-bridge.js`的實作模式整個重寫（面板純JS動態建立、style注入、
  pointerdown/pointermove拖曳機制），預設定位左下角避開模擬器的右下角。`index.html`只留一個
  header按鈕，面板本體不再是靜態HTML。自動帶入當前task的題目脈絡（description/examples），
  切換子任務自動重置對話。
- **測試結果**：白名單擋人、CORS擋不明來源、正常對話流程（含情境A模糊提問「我不會」的安撫+
  導入第一階段I/O拆解）、浮動視窗拖曳、與模擬器同時開啟不重疊、未載入課程時顯示友善提示並停用
  輸入框——全部在瀏覽器實測通過。AI回應完全依照V6.2的「三核心結構」格式（一句判斷/最多兩點提醒/
  一個引導問題）。
- 已commit（`a79b580`，14個檔案），**尚未push**，等使用者確認。`ALLOWED_COURSE_CODES`目前只開了
  `JSA00`（測試用），正式要開放哪些課程代碼待下次使用者決定。

**2026-07-22第十七輪：AI伴學面板加頭像、修縮放bug、正式部署上線**

- 使用者提供`AIlogo.png`／`STlogo.png`兩張圓形徽章風格頭像圖（各約1254x1254px、1.5~1.7MB），
  移到`public/avatars/`並用PowerShell內建`System.Drawing`（不需額外安裝套件）縮到160x160px，
  各約52KB（降幅97%），對話泡泡顯示的24~30px頭像解析度綽綽有餘、視覺品質未受影響。加進
  `src/aiCompanion.js`的聊天泡泡（AI在左、學生在右）與面板標題列小圖示，路徑一樣用
  `import.meta.env.BASE_URL`取得，跟模擬器面板同慣例。
- **使用者發現迴歸bug**：改成浮動視窗版本時，輸入框的`resize`被誤設成`none`（原本modal版本是
  `vertical`），且浮動面板本身（兩個版本）都從未支援整體縮放。已修正：面板加上`resize: both`
  （`min-width:300px／min-height:340px／max-width:92vw／max-height:92vh`），輸入框改回
  `resize: vertical`（`max-height`同時從120px放寬到240px，配合面板可以整體拉大）。瀏覽器實測
  兩種縮放都正常運作（面板從380x520成功拖到596x340，輸入框CSS屬性確認`resize:vertical`）。
- **確認GitHub Pages尚未同步過AI伴學功能**：使用者詢問「專案是否已部署，可以直接測試」時發現
  `gh-pages`分支停在7/21早上8:51，比AI互動模組所有commit（深夜才push）都舊。已依序
  `git push`＋`npm run deploy`兩輪（頭像功能一輪、縮放修正一輪），確認GitHub Pages CDN更新
  生效後正式網址`https://p2766602-beep.github.io/BlocklyYdws/`可以正常訪問並使用AI伴學（僅限
  `JSA00`課程，白名單其餘代碼待開放）。
- 使用者下班前確認本機環境已清理（背景dev server、殘留的`wrangler tail`程序皆已停止），
  git working tree乾淨、與origin/main同步無待推送內容。

**2026-07-22第十八輪：情境B（密室逃脫）架構規格定案，決定拆成獨立專案**

- 使用者確認：情境D（SRC助教/SRC00）維持留在BlocklyYdws主平台的AI伴學浮動面板內，用courseCode
  路由；但情境B（通用演算法密室，11主題、學生自選不綁定Blockly課程）決定拆成**獨立新專案**，
  比照`blockly-lab`/`YDWS-CodingBank`/`BlocklyYdws`三專案並存模式。
- 情境D跟情境B共用同一套密室逃脫引擎（情境D只是換SRC知識包），為避免重工與避免把複雜的關卡
  狀態機邏輯塞進情境C用的簡單聊天Worker，決定引擎程式碼只在獨立專案裡寫一份，主平台對SRC00
  改採iframe嵌入獨立專案的`embed.html`（比照`src/smartring/simulator-bridge.js`現有的
  iframe+postMessage模式），不是走`aiCompanion.js`現有的純文字chat fetch流程。
- 過程中修正了兩個先前提案的過度設計：①金鑰只是遊戲儀式感/動機工具（截圖領獎品用），不是安全
  憑證，不需要密碼學等級隨機，只需後端KV記錄關卡進度防止學生竄改前端history造假過關。②Level
  2/4/5（固定選項題型）原提案「答案判定完全交給後端deterministic比對、排除AI」是錯的，已修正
  為按鈕（快速路徑，後端比對correctIndex）與文字輸入（一律進AI判斷，處理學生自己的話/提問/
  部分理解）**雙軌並存**，按鈕不取代AI對自由文字回應的判斷責任。
- 情境C（AI伴學-Blockly解題陪伴）本身也確認一條規則：`docs/AI伴學GEM-V62-AI素養評估版.md`
  第二階段「學習概念連結」**不可跳過**（現行prompt本來就有這條硬規則，確認保留），其餘階段
  （I/O拆解/條件規則/人工兵推）可依學生明說「這關我知道了」或「已完成程式碼需要除錯」由AI
  判斷是否跳過，屬於情境C的prompt細節調整，跟情境B獨立專案化無關。
- 完整架構規格已寫成獨立文件：`docs/密室逃脫獨立專案-架構規格.md`。待使用者決定的後續事項見
  該文件第7節（repo路徑/GitHub帳號、Worker/KV命名、embed整合實測、教師後台範圍、年齡分版）。

**2026-07-23第二十輪：情境B（密室逃脫）11主題確定採「軟性推薦路徑」遊戲化總覽，MVP先做骨架不做美術地圖**

- 使用者提供一張RPG世界地圖風格參考圖（4區域練功房、編號引導路線），詢問情境B的11個主題是否能
  比照編序成遊戲化關卡地圖。評估後確認：11主題本身有隱性難度依存關係（陣列基礎→線性搜尋→
  排序→二分搜尋(需先懂排序)→貪婪→DFS→BFS→動態規劃(最難)），適合顯性化成建議路徑。
- **關鍵決定：軟性推薦路徑，不做硬鎖關**——地圖/技能樹上標示建議順序＋已完成進度，但任何節點
  皆可自由點擊進入，不因未完成前一主題而鎖住。理由：與既有「情境B學生自選主題、不綁定」的
  設計初衷相容，同時兼顧一般班（跟著建議路線走）與資優班（想跳關挑戰BFS/DP也行）兩種學生。
- **範圍決定：先做低成本MVP，美術地圖化列為之後可選加值項、不排入這輪範圍**。MVP只需：①後端在
  現有`(studentId, topic)`關卡進度基礎上加一層跨主題總覽讀取（掃描11主題各自完成狀態，非新的
  儲存格式）；②前端加一個技能樹/路徑圖總覽畫面（節點+連線+完成打勾+建議路徑高亮，用現有網頁
  排版風格即可，不做像素風地圖插畫，避免美術素材授權/成本問題）。
- 下一步待辦（尚未動工，待使用者評估後排入`docs/密室逃脫獨立專案-架構規格.md`第7節或另立
  section）：11主題最終建議順序拍板（含3種排序法要算1個節點還是3個節點）、「已完成」判定標準
  （5關全破 vs 部分關卡）、總覽畫面實際嵌入位置（需先確認YDWS-EscapeRoom repo目前主題選單頁的
  現況）。

**2026-07-23第二十一輪：情境B確認為資優班課程定位，主題從11個擴充為13個並定案學習順序**

- 使用者澄清：情境B（密室逃脫）是**資優班課程**，不需要為一般班保留簡化版路線，先前11個主題
  是使用者土法煉鋼自訂，請從程式設計教學的鷹架邏輯重新評估是否需要增刪。
- 評估後**新增2個主題**：①**遞迴基礎**（費氏數列/階乘一類經典題材，教「函式呼叫自己＋終止
  條件」）——原11主題裡DFS跟動態規劃都預設學生已懂遞迴，但沒有任何一關真正教過，容易造成
  斷層，補在二分搜尋之後、貪婪演算法之前當橋樑關卡。②**合併排序**——用剛學的遞迴回頭重新解
  排序問題，跟Tier2的三種排序法正面對照，呼應資優班加深需求。因確認是資優班課程，兩者皆設為
  **主線必過**，不做選修分支。**沒有刪除任何原有主題**——三種排序法看似重複但是刻意的策略
  對照組，予以保留。
- **定案13主題學習順序**（每關的技巧會被下一關重新利用或正面對照，非單純難度平鋪）：
  Tier1基礎(找最大值/找最小值/線性搜尋) → Tier2排序三部曲(氣泡/選擇/插入排序) →
  Tier3分治初體驗(二分搜尋) → Tier3.5遞迴橋樑(遞迴基礎/合併排序) →
  Tier4策略與圖走訪(貪婪演算法/DFS/BFS) → Tier5總整理(動態規劃，建議用「貪婪在此題會選錯」
  的題目切入)。DFS排BFS前面是設計取捨（承接Tier3.5剛建立的遞迴直覺），非唯一正解，未來如發現
  學生認知曲線不合可調整。
- 完整主題表＋設計邏輯已寫入`docs/密室逃脫獨立專案-架構規格.md`§1，遊戲化總覽MVP決策（第二十輪）
  同步更新為§8，原§7尚待決定事項改編號§9並補充年齡分版問題已因資優班定位釐清而降為次要。

**其他待確認事項**：
- 密室逃脫檢核（GEM引擎）的目標年齡內容難度，是否要依國小/國中分兩版。
- 評分規則：單純通過/失敗，還是依測資通過率/嘗試次數/AI引導次數加權？
- Google Sheet同步時機：即時 / 完成課程組才寫 / 教師批次匯出？（目前blockly-lab是「系統評分完成後」即可上傳，屬於接近即時）
- Sheet結構：全平台共用一份（分頁分班）vs 每班/每梯次各自一份？
- Google Sheet串接方式：Service Account vs 教師個人OAuth？（blockly-lab目前用Apps Script Web App，接近Service Account模式，可視為已回答）
