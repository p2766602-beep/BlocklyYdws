# 換機器交接文件（2026-08-01，家用電腦 → 學校電腦）

下次工作階段開始（不管人工接續、還是開新Claude Code對話）：**先讀這份文件 + `專案規劃摘要.md`
最上方「目前決策現況」表**，不要只看單一專案的git歷史就下結論——三專案各自獨立git歷史，
互相看不到對方，過去發生過只查一邊就誤判進度的狀況。

## 三專案現況（本次交接時已全部檢查並push）

| 專案 | 分支 | 未commit變更 | 未push commit |
|---|---|---|---|
| BlocklyYdws | main | 0 | 0（已push4個commit：`9181d01`/`aec5382`/`4eafcfd`/`aefb8ed`） |
| YDWS-CodingBank | master | 0 | 0（已push2個commit：`5c9d6cc`/`6f52bec`） |
| YDWS-EscapeRoom | main | 0 | 0（本次完全沒動到這個專案） |

三專案全部乾淨、已push。

## 這次做了什麼（完整脈絡見`專案規劃摘要.md`「目前決策現況」表對應列）

因應官方競賽練習平台（demo.csie.ntnu.edu.tw）更新輸入輸出規範（改用當🚩被點擊/詢問並等待/
詢問的答案/說出這套Scratch式積木），完成一整條因應鏈：

1. **BlocklyYdws引擎修正**：新增4個對應積木，顏色比照官方Blockly模式實測色值（土黃`#e6bf00`/
   水藍`#3373cc`/墨綠`#5ba58c`，不是Scratch模式的紫色，這點曾經一開始採樣錯模式）。用JSL01
   課程的A-08-0（找最大值）驗證通過，含綠旗缺失攔截、孤兒積木忠實執行（比照官方行為）。
2. **YDWS-CodingBank題庫整理**：新建`raw/master/`統一TXT原始題庫（舊`raw/YDWS-CodingData/`
   搬過去+160題原本沒有txt的PB06/blockly-lab原生題也轉成txt），清理71個樣板殘留測資、
   修正1個案例編號打字錯誤，發現並合併**56組**跨血統重複（不是一開始以為的47組——用檔名
   碰撞偵測會漏掉標題文字不同的重複，例如「找最大值」vs「查找最大值」，後來改用CSV題目ID
   重複次數直接掃描才抓全）。
3. **N+序列輸入格式盤點**：確認BlocklyYdws的「詢問並等待」讀取器是逐詞斷詞不是逐行，
   測資本身不需要因應新規範改寫，只有2題（`M3-02-06`、`WP-02`）題目說明文字需要補強。
4. **推廣到既有課程＋重新匯出＋部署**：`export_course_js.py`新增`COURSE_REQUIRES_GREEN_FLAG`
   機制（比照既有`COURSE_DESCRIPTION`模式），JSA00/JSB00（目前唯二公開的JS解題課程）套用
   綠旗規範；CPB00/CYELEMENT01/CYJUNIOR01因題庫去重同步了knowledge-point/難度標籤metadata；
   已重新匯出6個課程JS並`npm run build`+`npm run deploy`部署上線。

## 已知待辦（下一輪可以做，不急）

- JSA00/JSB00現有13題舊式`starterXml`範例積木、5題空白，都還沒改寫成新積木版本（詢問並
  等待/說出），這輪只確保引擎規範正確套用，範例積木維持原樣。
- `JSA01`/`JSB02`/`JSS01`/`JSL01`這4個JS開頭的課程，只有JSL01（試辦）跟JSA00/JSB00
  （公開課程優先）套用了綠旗規範，其餘還沒推廣。
- **⚠️ JSA00/JSB00現在系統評分要求綠旗積木，這是對既有公開課程的行為變更**，之前不需要
  綠旗也能評分。如果需要對學生公告或緩衝期，這輪還沒做。

## 使用者被跳過/拒絕的動作

無——本次session所有push/部署都經過使用者明確同意才執行。

## 下一步建議

依優先順序：JSA00/JSB00的starterXml改寫成新積木版本 → 視情況推廣到JSA01/JSB02/JSS01這幾個
隱藏課程 → 評估要不要正式把它們也列入公開選單。
